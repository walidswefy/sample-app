version: '2'
services:
  sampleapplication-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/sampleapplication-mongodb-config --bind_ip 0.0.0.0
  sampleapplication-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  sampleapplication-mongodb-config:
    image: mongo:4.2.7
    container_name: sampleapplication-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
